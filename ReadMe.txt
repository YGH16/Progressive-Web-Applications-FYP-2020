First step to running the application is to install the dependencies using:

npm install

Then start server using:

npm start

then open localhost:{port number} in the browser.

open the pages most popular, Business, Health, World and tech on the first visit in order to build the article storage.